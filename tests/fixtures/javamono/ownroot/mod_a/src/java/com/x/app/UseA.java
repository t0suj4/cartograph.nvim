package com.x.app;

import com.x.util.Util;

/** The importer's OWN source root holds a copy: that is the one it compiles against. */
public class UseA {
	public String go() { return Util.id("a"); }
}
